package com.sahil.NestedInterface;

public class Main {
    public static void main(String[] args) {
        Something obj = new Something();
        System.out.println(obj.isOdd(6));
    }
}

// nested interface can be private ,public or protected
// But normal interface can be only public or the default one

